rules_version = '2';

// Dev: any signed-in user can read/write. Replace with role-based rules before production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
